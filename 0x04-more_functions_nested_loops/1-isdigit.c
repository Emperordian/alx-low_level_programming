#include "main.h"

/**
 * _isdigit - Checks for a digits
 * @c: The character to be checked
 *
 * Return: 1 if c is a digit. 0 is otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
